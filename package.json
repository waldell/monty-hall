{
  "name": "monty-hall",
  "version": "0.1.0",
  "private": true,
  "proxy": "http://localhost:2448",
  "engines": {
    "node": ">=12.16.0"
  },
  "devDependencies": {
    "express": "^4.17.1"
  },
  "dependencies": {
    "node-sass": "^4.14.0",
    "react": "^16.13.1",
    "react-dom": "^16.13.1",
    "react-scripts": "5.0.1"
  },
  "scripts": {
    "server": "node ./server",
    "start": "react-scripts start",
    "build": "react-scripts build"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}